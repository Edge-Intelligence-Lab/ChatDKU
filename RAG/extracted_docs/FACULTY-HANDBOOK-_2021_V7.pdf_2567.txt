Servers and the applications must be designed so that passwords cannot be retrieved by anyone (including system administrators). (This should include a mechanism to ensure that access requires two-factor authentication, including the Duke Kunshan NetID plus a second-factor time-based code.) Interactive access to servers must be logged. The logs should include the identity of the user, the time and the function (login or logout).